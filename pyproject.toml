[tool.ruff]
target-version = "py311"
line-length = 120
select = ["I", "E", "W", "B", "F", "UP", "RUF", "PL", "Q"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "single"

[tool.pytest.ini_options]
asyncio_mode = "auto"
